function solution(dirs) {
    let answer = 0;
    const matrix = Array.from(Array(10), () => Array(10).fill(0));
    console.log(matrix);
    // dirs.split('').forEach((el) => console.log(el));
    return answer;
}
solution('ULURRDLLU');
